services:
  - type: web
    name: mytodolist
    env: docker
    region: frankfurt
    plan: free
    envVars:
      - key: MONGO_URI
        sync: false
      - key: SESSION_SECRET
        sync: false
