services:
  - type: web
    name: mytodolist
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "18"
      - key: MONGO_URI
        sync: false
      - key: SESSION_SECRET
        sync: false
